# Copy this file to atlas-credentials.env and fill it locally. Never commit it.
# The password is passed directly to the MongoDB driver, so do not URL-encode it here.
MONGODB_USERNAME=
MONGODB_PASSWORD=
MONGODB_URI=mongodb+srv://<cluster-host>/openbasket?retryWrites=true&w=majority
