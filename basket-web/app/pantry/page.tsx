import PlaceholderPage from '../../components/PlaceholderPage';

export default function PantryRoute() {
  return <PlaceholderPage title="My pantry" body="Receipt scanning will fill this digital pantry. Recipe matching already uses a mock pantry adapter so recommendations can ship first." />;
}
